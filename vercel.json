{
    "buildCommand": "pnpm turbo run build --filter=@shop-wise/web",
    "outputDirectory": "apps/web/dist",
    "framework": null,
    "installCommand": "pnpm install",
    "functions": {
        "apps/api/api/index.js": {
            "runtime": "nodejs18.x"
        }
    },
    "env": {
        "SERVERLESS": "true"
    },
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api"
        }
    ]
}
